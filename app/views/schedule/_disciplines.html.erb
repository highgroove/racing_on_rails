<%= tabs do |tabs|

  tabs.add('All', :discipline => nil, :year => @year)
  
  @disciplines = Discipline.find(:all, :order => 'name')

  @disciplines.each do |disc|
   tabs.add(disc.name , :discipline => disc.name.capitalize, :year => @year)
  end

end  %>

