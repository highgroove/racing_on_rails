GBRA Sponsors
</br>
<!-- NEW SPONSORS -->
<% @sponsors = Sponsor.where(:display => true) %>

<% @sponsors.each do |sponsor| %>
<%= link_to image_tag(sponsor.photo.url(:original)), sponsor.website %>
<% end %>
