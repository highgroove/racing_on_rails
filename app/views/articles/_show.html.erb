<div class="content-item">
  <br/>
  <%= link_to article.title, article, :class=>"title" %>
  <div class="story"><%= article.body.html_safe %></div>

  <% unless article.assets.empty? %>
  <h3>Attached Files</h3>
  <% article.assets.each do |asset| %>
  <p>
    <%= link_to(asset.asset_file_name, asset.asset.url(:original))%>
  </p>   
  <% end %>
  <% end %>



  <div class="timestamp">
    <ul>
      <li>Created: <%= article.created_at.to_formatted_s(:short) %></li>
      <li>Modified: <%= article.updated_at.to_formatted_s(:short) %></li>
    </ul>
  </div>
</div>
