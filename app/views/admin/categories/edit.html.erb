<h1>Editing category</h1>


<%= form_for([:admin, @category], :html => { :multipart => true }) do |f| %>
<%= render :partial => "form", :locals => { :f => f, :action_button => "Update" } %>
<% end %>


<div class="content-item">
<% #= link_to 'Delete', [:admin, @category], :confirm => 'Are you sure you want to delete this category?', :method => :delete %>

<%= link_to 'Back', admin_categories_path %>
</div>

